# « Corriger les CVE et redéployer » — Invoice-Service (mono-conteneur)
#
# DEUX FAÇONS DE LE DÉCLENCHER
#
#   1. workflow_dispatch — le bouton dans l'onglet Actions. Contrôle total :
#      tu choisis la stratégie et tu coches « deployer » pour publier.
#      C'est LE seul chemin qui peut déployer.
#
#   2. repository_dispatch (type: cve-detected) — appelable par API, prévu pour
#      l'outil de détection de CVE externe. TOUJOURS en essai à blanc :
#      il met à jour les deps, rebuild depuis une base fraîche, rejoue
#      audit + lint + génération de PDF + Trivy, et écrit le rapport dans le
#      résumé du job. Il ne publie RIEN et ne déploie RIEN.
#
#      Déclenchement :
#        gh api repos/${{ github.repository }}/dispatches \
#          -f event_type=cve-detected
#
#      Le token a besoin de la permission `contents: write` sur le repo.
#      Note GitHub : repository_dispatch ne s'exécute que sur la branche par
#      défaut, quelle que soit la branche visée.
#
# PAS DE CRON. Rien ne se déclenche tout seul dans le temps.
#
# ── ATTENTION PARTICULIÈRE : LE COUPLAGE PLAYWRIGHT ──────────────────────────
# Le paquet npm `playwright` est épinglé en 1.58.2 EXACT pour correspondre aux
# navigateurs de l'image mcr.microsoft.com/playwright:v1.58.2-noble. Les
# révisions de navigateurs sont liées à la version du paquet.
#
# La stratégie « patch-et-mineur » ne peut pas le bouger (version exacte).
# La stratégie « majeures-incluses » (`pnpm update --latest`) LE BOUGERAIT :
# une étape de contrôle échoue alors explicitement plutôt que de laisser partir
# une image incapable de générer des PDF. Pour monter Playwright, il faut
# modifier package.json ET le FROM du Dockerfile ensemble, à la main.

name: Corriger les CVE et redéployer

on:
  workflow_dispatch:
    inputs:
      strategie:
        description: "Ampleur de la mise à jour des dépendances"
        required: true
        default: patch-et-mineur
        type: choice
        options:
          # Respecte les plages semver déclarées dans package.json.
          # playwright y étant épinglé en version exacte, il n'est pas touché.
          - patch-et-mineur
          # `pnpm update --latest` : franchit les versions majeures.
          # Désalignera playwright de son image → échec contrôlé.
          - majeures-incluses
          # Ne touche à aucune dépendance : reconstruit seulement l'image depuis
          # une base fraîche (apt upgrade + couches Ubuntu à jour).
          - images-de-base-seules
      deployer:
        description: "Déployer si tout est propre (décocher pour un essai à blanc)"
        required: true
        default: true
        type: boolean

  repository_dispatch:
    types: [cve-detected]

env:
  REGISTRY: ghcr.io
  TRIVY_IMAGE: aquasec/trivy:0.73.0
  PNPM_VERSION: 10.33.4
  # Aligné sur le Node de l'image Playwright, pas sur les autres repos.
  NODE_VERSION: '24'

permissions:
  contents: read

concurrency:
  group: fix-cve-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  corriger:
    name: Corriger, tester, scanner, déployer
    runs-on: ubuntu-latest
    timeout-minutes: 50
    permissions:
      contents: write
      packages: write

    steps:
      # ── 0. Résolution des paramètres selon l'origine du déclenchement ───────
      # Le client_payload est une entrée externe : il est lu via une variable
      # d'environnement (jamais interpolé directement dans le shell, ce qui
      # serait une injection de script) et validé contre une liste blanche.
      - name: Résoudre les paramètres
        id: params
        env:
          EVENT: ${{ github.event_name }}
          IN_STRATEGIE: ${{ inputs.strategie }}
          IN_DEPLOYER: ${{ inputs.deployer }}
          CP_STRATEGIE: ${{ github.event.client_payload.strategie }}
          CP_RAISON: ${{ github.event.client_payload.raison }}
        run: |
          set -euo pipefail
          if [ "$EVENT" = "repository_dispatch" ]; then
            STRATEGIE="${CP_STRATEGIE:-patch-et-mineur}"
            # Verrouillé : un déclenchement API ne déploie jamais.
            DEPLOYER="false"
            ORIGINE="API (repository_dispatch) — essai à blanc forcé"
          else
            STRATEGIE="${IN_STRATEGIE}"
            DEPLOYER="${IN_DEPLOYER}"
            ORIGINE="Manuel (workflow_dispatch)"
          fi

          case "$STRATEGIE" in
            patch-et-mineur|majeures-incluses|images-de-base-seules) ;;
            *)
              echo "::error::Stratégie invalide : « ${STRATEGIE} »."
              exit 1
              ;;
          esac

          echo "strategie=$STRATEGIE" >> "$GITHUB_OUTPUT"
          echo "deployer=$DEPLOYER"   >> "$GITHUB_OUTPUT"

          {
            echo "### Déclenchement"
            echo "- Origine : ${ORIGINE}"
            echo "- Stratégie : \`${STRATEGIE}\`"
            echo "- Déploiement : $([ "$DEPLOYER" = "true" ] && echo 'oui' || echo 'non — essai à blanc')"
            if [ -n "${CP_RAISON:-}" ]; then
              echo "- Raison transmise par l'outil : ${CP_RAISON}"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      # ── 1. Mise à jour des dépendances ──────────────────────────────────────
      # `pnpm update` respecte .npmrc, donc minimum-release-age=1440 : aucune
      # version publiée depuis moins de 24 h n'est retenue, y compris ici.
      - name: Mettre à jour les dépendances
        id: maj
        env:
          STRATEGIE: ${{ steps.params.outputs.strategie }}
        run: |
          set -euo pipefail
          if [ "$STRATEGIE" = "images-de-base-seules" ]; then
            echo "Stratégie « images-de-base-seules » : aucune dépendance touchée."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          FLAGS=""
          [ "$STRATEGIE" = "majeures-incluses" ] && FLAGS="--latest"

          pnpm install --frozen-lockfile
          pnpm update $FLAGS

          if git diff --quiet -- package.json pnpm-lock.yaml; then
            echo "Aucune mise à jour disponible."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            {
              echo "### Dépendances"
              echo "Aucune mise à jour disponible sous cette stratégie."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            {
              echo "### Dépendances mises à jour"
              echo '```'
              git diff --stat -- package.json pnpm-lock.yaml
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      # Contrôle critique : si la mise à jour a désaligné playwright de son
      # image, on s'arrête ici. Une image dont le paquet et les navigateurs ne
      # correspondent pas ne génère plus de PDF.
      - name: Vérifier que playwright est aligné sur l'image Playwright
        run: |
          set -euo pipefail
          PKG=$(node -p "require('./package.json').dependencies.playwright")
          IMG=$(grep -oE 'playwright:v[0-9]+\.[0-9]+\.[0-9]+' Dockerfile | head -1 | sed 's/playwright:v//')
          echo "package.json : $PKG"
          echo "Dockerfile   : $IMG"
          if [ "$PKG" != "$IMG" ]; then
            {
              echo "### Désalignement Playwright — arrêt"
              echo ""
              echo "- package.json : \`${PKG}\`"
              echo "- image Docker : \`v${IMG}\`"
              echo ""
              echo "Les révisions de navigateurs sont liées à la version du paquet."
              echo "Pour monter Playwright, modifie **package.json ET le FROM du Dockerfile** ensemble, à la main."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "::error::Désalignement Playwright : package.json '${PKG}' vs image v${IMG}."
            exit 1
          fi
          echo "Alignés."

      # ── 2. Contrôles qualité et sécurité ────────────────────────────────────
      - name: Install
        run: pnpm install --no-frozen-lockfile

      - name: Audit (prod, high+)
        run: pnpm audit --prod --audit-level high

      - name: Lint
        run: pnpm lint

      - name: Test (if present)
        run: pnpm run --if-present test

      # ── 3. Version et build de l'image ──────────────────────────────────────
      - name: Bump version
        id: tag
        run: |
          set -euo pipefail
          CONTENT=$(cat version.txt | tr -d '\r\n ')
          MAJOR=$(echo "$CONTENT" | cut -d. -f1)
          MINOR=$(echo "$CONTENT" | cut -d. -f2)
          PATCH=$(echo "$CONTENT" | cut -d. -f3)
          if ! [[ "$MAJOR" =~ ^[0-9]+$ ]] || ! [[ "$MINOR" =~ ^[0-9]+$ ]] || ! [[ "$PATCH" =~ ^[0-9]+$ ]]; then
            echo "::error::version.txt invalide: $CONTENT (attendu X.Y.Z)"; exit 1
          fi
          if [ "$PATCH" -lt 99 ]; then PATCH=$((PATCH + 1)); else PATCH=0; MINOR=$((MINOR + 1)); fi
          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "$NEW_VERSION" > version.txt
          echo "tag=v${NEW_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # `pull: true` + `no-cache: true` : sans eux, Docker réutiliserait les
      # couches Ubuntu en cache, correctifs apt compris — c'est-à-dire non
      # compris.
      #
      # Build UNIQUE chargé en local : c'est ce binaire précis qui sera scanné
      # puis, le cas échéant, publié tel quel.
      - name: Build (local)
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          push: false
          tags: invoice-service:candidate
          pull: true
          no-cache: true

      # Le seul contrôle fonctionnel qui compte pour ce service, et qu'aucun
      # test unitaire ne couvre.
      - name: Vérifier la génération de PDF dans l'image
        run: |
          docker run --rm --entrypoint node invoice-service:candidate -e "
            import('playwright').then(async ({chromium}) => {
              const b = await chromium.launch();
              const p = await b.newPage();
              await p.setContent('<h1>smoke test</h1>');
              const pdf = await p.pdf({ format: 'A4' });
              if (!pdf || pdf.length < 500) throw new Error('PDF vide ou tronqué');
              console.log('PDF généré :', pdf.length, 'octets');
              await b.close();
            }).catch(e => { console.error('Échec Playwright :', e.message); process.exit(1); });
          "

      # ── 4. Scan Trivy AVANT publication ─────────────────────────────────────
      - name: Trivy
        id: trivy
        run: |
          set -uo pipefail
          FAIL=0
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
            ${{ env.TRIVY_IMAGE }} image --exit-code 1 --severity CRITICAL,HIGH \
            --ignore-unfixed --skip-version-check --format table --timeout 20m \
            invoice-service:candidate | tee /tmp/trivy.txt || FAIL=1

          {
            echo "### Scan Trivy"
            if [ "$FAIL" = "0" ]; then
              echo "Aucune vulnérabilité CRITICAL ou HIGH corrigeable."
              echo ""
              echo "_Rappel : \`--ignore-unfixed\` masque les CVE sans correctif disponible._"
              echo "_Un scan vert veut dire « rien de corrigeable », pas « zéro CVE »._"
            else
              echo "**Vulnérabilités restantes — publication annulée.**"
              echo '```'
              grep -hE "CRITICAL|HIGH" /tmp/trivy.txt | head -40 || true
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

          exit "$FAIL"

      # ── 5. Publication et déploiement (workflow_dispatch + deployer coché) ──
      - name: Log in to GHCR
        if: steps.params.outputs.deployer == 'true'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push
        if: steps.params.outputs.deployer == 'true'
        id: push
        env:
          TARGET: ${{ env.REGISTRY }}/${{ github.repository_owner }}/invoice-service:${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          docker tag invoice-service:candidate "$TARGET"
          PUSH_OUT=$(docker push "$TARGET" | tee /dev/stderr)
          DIGEST=$(docker buildx imagetools inspect "$TARGET" --format '{{.Manifest.Digest}}' 2>/dev/null || true)
          if [ -z "$DIGEST" ]; then
            DIGEST=$(printf '%s\n' "$PUSH_OUT" | grep -oE 'sha256:[0-9a-f]{64}' | tail -1 || true)
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      # Le pin du digest est ce qu'ArgoCD observe : c'est cette étape, et pas le
      # push GHCR, qui déclenche réellement le déploiement.
      - name: Pin digest + commit
        if: steps.params.outputs.deployer == 'true'
        env:
          TAG: ${{ steps.tag.outputs.tag }}
          BRANCH: ${{ github.ref_name }}
          DIGEST: ${{ steps.push.outputs.digest }}
          OWNER: ${{ github.repository_owner }}
          REG: ${{ env.REGISTRY }}
        run: |
          set -euo pipefail
          VALUES_PATH="charts/invoice-service/values.yaml"

          if [ -n "${DIGEST:-}" ]; then
            IMAGE="${REG}/${OWNER}/invoice-service@${DIGEST}"
          else
            IMAGE="${REG}/${OWNER}/invoice-service:${TAG}"
            echo "::warning::Digest introuvable, pin sur le tag ${TAG}."
          fi

          if ! grep -qE "^[[:space:]]+image:[[:space:]]*ghcr\.io/.*invoice-service[@:]" "$VALUES_PATH"; then
            echo "::error::Aucune ligne 'image:' pour invoice-service dans ${VALUES_PATH}. Le pin de digest aurait échoué en silence."
            exit 1
          fi
          sed -i -E "s|^([[:space:]]+)image:[[:space:]]*ghcr\.io/.*invoice-service[@:].*|\1image: ${IMAGE}|" "$VALUES_PATH"
          if ! grep -qF "image: ${IMAGE}" "$VALUES_PATH"; then
            echo "::error::Le remplacement n'a pas pris dans ${VALUES_PATH}."
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add version.txt package.json pnpm-lock.yaml "$VALUES_PATH"

          if git diff --staged --quiet; then
            echo "::warning::Rien à commiter."
          else
            git commit -m "fix(sécurité): correction CVE et rebuild ${TAG} [skip ci]"

            pushed=false
            for attempt in 1 2 3; do
              if git push origin "HEAD:${BRANCH}"; then
                pushed=true
                break
              fi
              echo "::warning::Push refusé (tentative ${attempt}/3), rebase sur origin/${BRANCH}…"
              git fetch origin "$BRANCH"
              if ! git rebase "origin/${BRANCH}"; then
                git rebase --abort || true
                echo "::error::Conflit de rebase sur ${BRANCH}, intervention manuelle requise."
                exit 1
              fi
              sleep $((attempt * 3))
            done
            if [ "$pushed" != "true" ]; then
              echo "::error::Impossible de pousser après 3 tentatives."
              exit 1
            fi
          fi

          {
            echo "### Déploiement"
            echo "- Version : \`${TAG}\`"
            echo "- Image : \`${IMAGE}\`"
            echo ""
            echo "ArgoCD synchronise le cluster dans les minutes qui suivent."
          } >> "$GITHUB_STEP_SUMMARY"

      # ── 6. Essai à blanc : rapport et marche à suivre ────────────────────────
      - name: Essai à blanc — rien n'a été publié
        if: steps.params.outputs.deployer != 'true'
        env:
          STRATEGIE: ${{ steps.params.outputs.strategie }}
          CHANGED: ${{ steps.maj.outputs.changed }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/workflows/fix-cve.yml
        run: |
          {
            echo "### Essai à blanc — validation manuelle requise"
            echo ""
            echo "Audit, lint, génération de PDF et scan Trivy sont **passés**. Aucune image publiée, aucun déploiement."
            echo ""
            if [ "${CHANGED:-false}" = "true" ]; then
              echo "Des dépendances ont été mises à jour (voir le diff plus haut) mais **rien n'a été commité** :"
              echo "ces changements sont perdus à la fin du job. Ils seront recalculés à l'identique au prochain lancement."
            else
              echo "Aucune dépendance n'a bougé — seule l'image de base a été reconstruite."
            fi
            echo ""
            echo "**Pour appliquer :**"
            echo "1. Ouvrir [le workflow](${RUN_URL})"
            echo "2. « Run workflow » → stratégie \`${STRATEGIE}\` → cocher **deployer**"
            echo ""
            echo "_Un déclenchement par API ne peut pas déployer : c'est volontaire._"
          } >> "$GITHUB_STEP_SUMMARY"
